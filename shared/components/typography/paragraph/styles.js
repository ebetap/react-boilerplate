const styles = theme => ({
  root: {
    fontSize: theme.utils.toRem(15),
    lineHeight: 1.5,
    color: '#333',
    margin: 0,
    marginBottom: theme.utils.toRem(15),
  },
});

export default styles;
