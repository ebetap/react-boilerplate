const styles = theme => ({
  root: {
    fontSize: theme.utils.toRem(18),
    lineHeight: 1.35,
    fontWeight: 500,
    color: '#3c4857',
    marginBottom: theme.utils.toRem(5),
  },
});

export default styles;
